apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config # Name of the ConfigMap (used for reference in Deployment) [!!! important !!!]
data: # Key-value pairs of configuration data
  ENV: "development"
  LOG_LEVEL: "debug"
  DATABASE_HOST: "postgres.default.svc.cluster.local"
